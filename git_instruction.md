# Git Quick Guide for LaTeX Project

## 1. Get the latest changes
```bash
git pull
````

## 2. Create a new branch

```bash
git checkout -b new-branch
```

## 3. Switch back to main

```bash
git checkout main
```

## 4. Push your branch

```bash
git push -u origin new-branch
```

## 5. Create a Pull Request on GitHub

1. Go to the repository on GitHub.
2. You should see a prompt for your pushed branch. Click **Compare & pull request**.
3. Add a short title and description.
4. Click **Create pull request**.

---
# Workflow

## Create new branch

```bash
git checkout main
git pull
git checkout -b my-update
# make your changes
git push -u origin my-update
```

---

## Update min branch

```
git checkout main

git pull
```
